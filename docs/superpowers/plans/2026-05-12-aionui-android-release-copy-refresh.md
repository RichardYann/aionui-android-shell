# AionUi Android Release Copy Refresh Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refresh the Android release title and release notes into a polished bilingual format, and rewrite the repository homepage README into a more product-showcase oriented landing page.

**Architecture:** Keep the working release automation unchanged except for release copy generation in `.github/workflows/android-release.yml`. Update the repository homepage copy in the root `README.md`, while leaving `android-shell/README.md` as the module-level technical note unless a tiny consistency tweak becomes necessary later.

**Tech Stack:** GitHub Actions YAML, Markdown, GitHub Release via `softprops/action-gh-release@v2`, GitHub CLI or GitHub MCP for CI-only verification

---

## File Structure

- Modify: `.github/workflows/android-release.yml`
  - Replace the plain release title and autogenerated-only notes with a bilingual markdown template generated from the workflow version input
- Modify: `README.md`
  - Rewrite the repository homepage into a product-showcase style bilingual landing page with moderate emoji use
- Keep: `android-shell/README.md`
  - Preserve as the module-level technical README unless a later consistency issue is proven

## Execution Constraint

This plan must continue to follow the user’s explicit rule:

- Do not run local Gradle builds or local verification builds
- Validate only through GitHub Actions, GitHub Release inspection, and repository file inspection

Implementation note:

- The remote repository already contains a root `README.md`, but the current local workspace does not expose that file through normal file reads
- If this mismatch remains during execution, update the root `README.md` through GitHub MCP using the remote file content as the baseline instead of mistakenly editing `android-shell/README.md`

### Task 1: Replace Release Title And Release Notes Template

**Files:**
- Modify: `.github/workflows/android-release.yml`

- [ ] **Step 1: Replace the release title expression with the bilingual product title**

Update the `Create GitHub Release` step from:

```yaml
      - name: Create GitHub Release
        if: ${{ success() }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: ${{ env.RELEASE_TAG }}
          files: ${{ env.RELEASE_FILE }}
          generate_release_notes: true
```

to:

```yaml
      - name: Create GitHub Release
        if: ${{ success() }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: AionUi Shell for Android / AionUi Android 客户端 ${{ env.RELEASE_TAG }}
          files: ${{ env.RELEASE_FILE }}
          body: |
            ## Overview / 版本说明

            AionUi Shell for Android is a lightweight Android shell for AionUi WebUI.
            AionUi Android 客户端是一个面向 AionUi WebUI 的轻量 Android 壳应用。

            ## Highlights / 本版亮点

            - Connect to your AionUi server quickly and reopen the last server on later launches
            - Immersive top navigation that stays out of the way and reveals on demand
            - Clear loading feedback during connection and refresh
            - Supports file upload and Android system downloads
            - Updated launcher branding with the AionUI logo
            - 快速连接你的 AionUi 服务器，并在下次启动时自动重新打开最近一次连接
            - 顶部导航更沉浸，需要时再唤出，不打断页面内容
            - 连接与刷新过程中提供清晰的加载反馈
            - 支持文件上传与 Android 系统下载
            - 启动图标已更新为 AionUI 品牌标识

            ## Downloads / 下载内容

            - Release asset: `${{ env.RELEASE_FILE }}`
            - 下载文件：`${{ env.RELEASE_FILE }}`

            ## Install / 安装方式

            - Install this build by downloading the APK asset from this release page
            - If Android blocks the installation, allow installation from unknown sources for this install
            - 可直接从本发布页下载 APK 安装
            - 如果系统拦截安装，请为本次安装允许“未知来源应用安装”

            ## Current Limitations / 当前限制

            - This stage prioritizes release automation first
            - Formal production signing will be added later
            - 当前阶段优先完成自动化发布链路
            - 正式生产签名将在后续版本补充

            ## Changelog / 更新记录

            - Full changelog: https://github.com/RichardYann/aionui-android-shell/commits/${{ env.RELEASE_TAG }}
            - 完整更新记录：https://github.com/RichardYann/aionui-android-shell/commits/${{ env.RELEASE_TAG }}
```

- [ ] **Step 2: Read the workflow file back and verify the template fields match the existing env names**

Check that the final workflow still uses:

```yaml
    env:
      RELEASE_VERSION: ${{ github.event.inputs.version }}
      RELEASE_TAG: v${{ github.event.inputs.version }}
      RELEASE_FILE: aionui-shell-v${{ github.event.inputs.version }}.apk
```

Expected result:

- `RELEASE_TAG` is still used in the release title
- `RELEASE_FILE` is still used in the body and uploaded files
- `generate_release_notes: true` is removed so the curated bilingual body is authoritative

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/android-release.yml
git commit -m "docs(ci): add bilingual android release copy"
```

### Task 2: Rewrite The Repository Homepage README

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Replace the current minimal root README with a bilingual product-showcase layout**

Use this content:

```md
# AionUi Shell for Android

Lightweight Android shell for AionUi WebUI.  
一个面向 AionUi WebUI 的轻量 Android 客户端。

Bring your self-hosted AionUi experience to Android with a focused WebView shell designed for quick access, immersive navigation, and practical daily use.  
将你的自托管 AionUi 体验带到 Android 设备上，用更轻量、更顺手的方式快速访问、沉浸浏览并完成日常使用。

## ✨ Features / 功能

- Connect to any AionUi WebUI server through a simple first-run setup
- Automatically reopen the last connected server on later launches
- Navigate with back, refresh, and change-server controls
- Support file upload through standard web file inputs
- Download files through Android system DownloadManager
- 首次启动可快速输入并连接任意 AionUi WebUI 地址
- 后续启动可自动重新打开最近一次连接的服务器
- 提供返回、刷新、切换服务器等常用导航能力
- 支持网页中的标准文件上传能力
- 支持通过 Android 系统下载管理器下载文件

## 🌊 Highlights / 特点

- Lightweight and focused: open your AionUi workspace without extra native complexity
- Immersive navigation: the top bar stays hidden until you intentionally reveal it
- Clear loading feedback: connection and refresh states are easier to understand
- Self-hosted friendly: simple URL-based access flow for personal deployments
- AionUI branding: launcher icon and app presence now better reflect the product identity
- 轻量专注：无需复杂原生层，就能快速进入 AionUi 工作界面
- 沉浸体验：顶部导航默认隐藏，需要时再唤出
- 加载清晰：连接与刷新状态更容易理解
- 自托管友好：适合个人或团队通过 URL 快速接入
- 品牌一致：图标与整体观感更贴近 AionUI 产品形象

## 📦 Release / 发布下载

- Latest releases: https://github.com/RichardYann/aionui-android-shell/releases
- APK naming pattern: `aionui-shell-v<version>.apk`
- Current release flow prioritizes automation first, with formal production signing to be added later
- 最新版本下载入口：https://github.com/RichardYann/aionui-android-shell/releases
- APK 命名规则：`aionui-shell-v<version>.apk`
- 当前发布流程优先打通自动化链路，正式生产签名将在后续补充

## 🛠️ Build / 构建

### Open in Android Studio

1. Open `android-shell/`
2. Let Android Studio sync Gradle
3. Run on a device or emulator

### GitHub Actions

- Debug workflow: `.github/workflows/android-shell-build.yml`
- Release workflow: `.github/workflows/android-release.yml`
- Release artifact: `aionui-shell-v<version>.apk`

### Local APK Outputs

- Debug: `android-shell/app/build/outputs/apk/debug/app-debug.apk`
- Release: `android-shell/app/build/outputs/apk/release/app-release.apk`
```

- [ ] **Step 2: If local root `README.md` is still unavailable, update the remote root README through GitHub MCP using the current remote content SHA as the baseline**

Use the remote file information already confirmed during brainstorming:

```json
{
  "path": "README.md",
  "sha": "68b1fa4cd1eb04771765da41424805b1257dd593"
}
```

Expected result:

- the repository homepage changes
- `android-shell/README.md` remains unchanged for now

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: refresh repository homepage copy"
```

### Task 3: Verify Through GitHub Release And Repository Pages

**Files:**
- Modify: `.github/workflows/android-release.yml` if copy fixes are needed
- Modify: `README.md` if wording fixes are needed

- [ ] **Step 1: Trigger the release workflow manually with a new test version**

Run through GitHub Actions with a fresh version such as `0.1.1`.

Expected result:

- workflow succeeds
- a new GitHub Release is created
- the release title is bilingual
- the release body matches the curated bilingual template

- [ ] **Step 2: Inspect the new release page through GitHub CLI or GitHub API**

Check:

- release title includes both English and Chinese
- the `Overview`, `Highlights`, `Downloads`, `Install`, `Current Limitations`, and `Changelog` sections appear
- the asset name is still `aionui-shell-v<version>.apk`

- [ ] **Step 3: Inspect the repository homepage rendering**

Check:

- the root `README.md` renders as the repository homepage
- bilingual intro, feature blocks, highlights, release section, and build section appear in the intended order
- emoji usage feels moderate and readable

- [ ] **Step 4: Apply wording-only fixes if necessary and re-run only through GitHub Actions**

Typical fix areas:

- release title readability
- markdown section spacing
- overly long bullets
- README line breaks and readability

- [ ] **Step 5: Commit each wording fix as a focused docs or ci/docs change**

```bash
git add .github/workflows/android-release.yml README.md
git commit -m "docs: polish android release and homepage copy"
```

## Plan Self-Review

- Spec coverage:
  - Bilingual release title: Task 1
  - Bilingual curated release body: Task 1
  - Product-showcase repository homepage: Task 2
  - Moderate emoji use: Task 2 and Task 3
  - CI-only validation: Task 3
- Placeholder scan:
  - No TODO, TBD, or vague implementation-only placeholders remain
  - The remote root README mismatch is addressed explicitly instead of left implicit
- Type consistency:
  - `RELEASE_TAG` and `RELEASE_FILE` are reused consistently in the release template
  - The README target is consistently the repository root `README.md`, not `android-shell/README.md`

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-12-aionui-android-release-copy-refresh.md`.

Two execution options:

1. `Subagent-Driven (recommended)`
   - I dispatch a fresh implementation subagent per task
   - I review between tasks and keep each change focused
   - CI debugging stays isolated and easier to recover from

2. `Inline Execution`
   - I execute the plan directly in this session
   - I still validate only through GitHub Actions and GitHub inspection
   - This keeps everything in one thread but makes the session more stateful
